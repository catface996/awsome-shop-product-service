<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.awsome.shop.product.repository.mysql.mapper.stock.StockReservationMapper">

    <!-- 按 (orderRef, productId) 查询预占（幂等去重） -->
    <select id="selectByOrderRefAndProductId"
            resultType="com.awsome.shop.product.repository.mysql.po.stock.StockReservationPO">
        SELECT id, order_ref, product_id, quantity, status,
               created_at, updated_at, created_by, updated_by, deleted, version
        FROM stock_reservation
        WHERE order_ref = #{orderRef}
          AND product_id = #{productId}
          AND deleted = 0
        LIMIT 1
    </select>

    <!-- 统计某商品 RESERVED 状态的预占总量 -->
    <select id="sumReservedQuantity" resultType="int">
        SELECT COALESCE(SUM(quantity), 0)
        FROM stock_reservation
        WHERE product_id = #{productId}
          AND status = 'RESERVED'
          AND deleted = 0
    </select>

    <!-- 批量统计多个商品 RESERVED 状态的预占总量 -->
    <select id="sumReservedByProductIds" resultType="map">
        SELECT product_id AS productId, COALESCE(SUM(quantity), 0) AS total
        FROM stock_reservation
        WHERE status = 'RESERVED'
          AND deleted = 0
          AND product_id IN
        <foreach collection="productIds" item="pid" open="(" separator="," close=")">
            #{pid}
        </foreach>
        GROUP BY product_id
    </select>

    <!-- 更新预占状态 -->
    <update id="updateStatusById">
        UPDATE stock_reservation
           SET status = #{status},
               updated_at = NOW()
         WHERE id = #{reservationId}
           AND deleted = 0
    </update>

</mapper>
